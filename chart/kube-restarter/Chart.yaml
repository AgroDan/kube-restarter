apiVersion: v2
name: kube-restarter
description: WatchTower-style pod updater for Kubernetes — restarts pods when their container images are updated in the registry
type: application
version: 0.1.0
appVersion: "0.1.0"
